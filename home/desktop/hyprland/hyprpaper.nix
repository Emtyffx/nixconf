{ config, pkgs, ... }:
{
  # services.hyprpaper.enable = true;
  # services.hyprpaper.settings = {
  #   splash = false;
  #   preload = [ "${./bg.jpg}" ];
  #   wallpaper = [ "DP-5,${./bg.jpg}" ];
  # };
}
